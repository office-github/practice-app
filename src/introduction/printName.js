import React from 'react';

export const name = "Bijay Kumar Yadav";
export const element = (
    <div>
        <h1 tabIndex="0">Elements that do not support the tabIndex attribute or support it and assign tabIndex to 0, in the order they appear.</h1>
        <h1 tabIndex="4">Hello, {name}</h1>
        <h1 tabIndex="1">Hello, {name}</h1>
        <h1 tabIndex="2">Hello, {name}</h1>
        <h1 tabIndex="3">Hello, {name}</h1>
    </div>
);
